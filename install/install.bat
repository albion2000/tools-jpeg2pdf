rem This is to do once after the python installation
pip install img2pdf
pip install unidecode
pip install pillow
pip install PyPDF2
rem wand is needed only for the tool scandirpdf2cover that extracts covers from pdfs 
pip install wand
@PAUSE
